# OpenAI — get yours at platform.openai.com
OPENAI_API_KEY=sk-...

# Paths — where data is stored (relative to project root)
FAISS_INDEX_PATH=data/faiss_index
SQLITE_DB_PATH=data/fincopilot.db
UPLOAD_DIR=data/uploads

# News API — get free key at newsapi.org (optional for now)
NEWS_API_KEY=your_newsapi_key_here

# Phase 2 — API settings
API_HOST=0.0.0.0
API_PORT=8000
API_SECRET_KEY=change_this_to_something_random